# Cohere API Configuration
# Required: Get your API key from https://cohere.com/
COHERE_API_KEY=your_cohere_api_key_here

# Optional: Cohere API timeout in seconds (default: 30)
COHERE_TIMEOUT=30

# CORS Configuration
# Development: Allow all origins (default)
# CORS_ORIGINS=*

# Production: Specify allowed origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Optional: API Base URL for demo scripts (default: http://localhost:8000/api/v1)
# Only needed if running the demo script against a different endpoint
# API_BASE_URL=http://localhost:8000/api/v1

